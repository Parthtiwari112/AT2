# run.py
print("To run the API locally:")
print("1) Create venv and install dependencies: python -m venv venv && source venv/bin/activate && pip install -r requirements.txt")
print("2) Run: uvicorn app.main_impl:app --reload --host 0.0.0.0 --port 8000")
